
const Banner = ({item}) => {

    return ( 
        <div className="banner"
        style={{backgroundImage:
            'url('+``+')',
            
            
            }}
        >

        </div>
    );
}

export default Banner;